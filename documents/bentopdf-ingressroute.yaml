apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: bentopdf-router
  namespace: documents
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`bentopdf.attias.io`)
      kind: Rule
      services:
        - name: bentopdf
          port: 3183
      priority: 10
      middlewares:
        - name: authentik
          namespace: traefik
    - kind: Rule
      match: "Host(`bentopdf.attias.io`) && PathPrefix(`/outpost.goauthentik.io/`)"
      priority: 15
      services:
          - kind: Service
            name: ak-outpost-authentik-embedded-outpost
            namespace: authentik
            port: 9000
  tls:
    certResolver: le