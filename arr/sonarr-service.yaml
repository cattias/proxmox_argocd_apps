apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: arr
  labels:
    app.kubernetes.io/name: sonarr
spec:
  type: ClusterIP
  ports:
    - name: "8989"
      port: 8989
      targetPort: 8989
  selector:
    app.kubernetes.io/name: sonarr