apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-config-pvc
  namespace: arr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-ceph
  resources:
    requests:
      storage: 1Gi