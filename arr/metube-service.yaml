apiVersion: v1
kind: Service
metadata:
  name: metube
  namespace: arr
  labels:
    app.kubernetes.io/name: metube
spec:
  type: ClusterIP
  ports:
    - name: "8081"
      port: 8081
      targetPort: 8081
  selector:
    app.kubernetes.io/name: metube